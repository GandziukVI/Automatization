#ifndef DEFINITIONS_H
#define DEFINITIONS_H

enum SourceMode
{
    Voltage,
    Current,
    ModeNotSet
};

enum SenseMode
{
    Voltage,
    Current,
    Resistance,
    ModeNotSet
};

enum OhmsMode
{
    Auto,
    Manual,
    ModeNotSet
};

enum AutoZeroMode
{
    ON,
    OFF
};

#endif // DEFINITIONS_H

